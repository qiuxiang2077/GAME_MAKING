# openspec/ — 变更管理

OpenSpec 工作流，用于复杂功能的规划与追踪。

| 内容 | 说明 |
|-----|------|
| config.yaml | OpenSpec 全局配置 |
| changes/ | 变更目录，每个变更含 proposal → design → tasks |

## 工作流

explore(分析) → propose(提案) → apply(执行) → archive(归档)

> 当前活跃变更：`define-core-gameplay-mechanics`（核心玩法机制）
